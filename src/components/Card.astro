---
const frontmatter = Astro.props;

const { title, image, alt, testItem1 } = frontmatter;
---

<div id="card-wrapper" class="bg- text- rounded-2xl sq-styles">
  <!-- ?: CHANGE BTNS TO CATEGORIES v-->
  <!-- ?:  ^-->
  <a href="/test">
    <!-- <img
      src={image}
      class="2xl:h-[300px] w-full rounded-tl-2xl rounded-tr-2xl sq-styles"
      alt={alt}
    /> -->
    <div class="bg- md:text-lg text-sm pt-2 p-3 md:ml-3 mr-2">
      <h3 class="font-bold lg:text-2xl text-lg">{title}</h3>
      <p class="">{testItem1}</p>
    </div>
  </a>
</div>
